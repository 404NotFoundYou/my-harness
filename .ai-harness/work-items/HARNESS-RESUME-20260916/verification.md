# 验证与交付边界

最终产品快照：`10a416ee54ce2a0dd5f0532c3b3f3557d186e58c07fdc2c5359547e4373bf09a`。本地Windows、Node20.20.2，TEMP/TMP使用D盘。revision3全部计划检查与独立复核对应同一快照，运行前后源码未变化。

| 检查 | 结果 | 命令证据 |
| --- | --- | --- |
| V1 新增实验续跑和中断边界 | 13/13通过 | 97769f2a-d9ea-4b6e-b96f-3c0a1d428519 |
| V2 原框架、多文件题及历史审计 | 23/23通过 | 9ca21c93-1369-42aa-a4de-eba881be0a78 |
| V3 Runtime稳定性 | 12/12通过 | 3ae4d2e1-6ecd-4d31-aa17-ee6882c9bc59 |
| V4 安装回归 | 20/20通过 | cdb59f63-c5b3-413d-aab3-1ac5b7a18826 |
| V5 其余Runtime测试文件 | 128/128通过 | ce8658b1-177a-48a1-9923-29df857b8786 |

覆盖全部25个Runtime测试文件160项、3个评测框架测试文件36项，合计196/196通过，零失败、跳过、取消或超时。旧两轮18样本和v2/v3合成协议审计继续通过。独立复核与git diff --check通过，见review.md。

新增9项首先在旧实现上全部失败；实现后通过并扩展边界。旧runCase夹具因新禁止覆盖语义出现EEXIST，已改用临时容器内未创建的子目录。独立审查先后复现两项P1：调用方修改原plan会改变实际模型/预算，以及CLI入口漂移后先发布的result可能被恢复逻辑误认completed；均通过CLI checkpoint/reopen保留失败与旧修订，修复后在revision3完整重验。

新增验证覆盖已完成失败样本不重复调用、未知中断不重试、只续未启动项、调用前停止、摘要损坏重建、整份ledger/结果预检、pending已有目录、completed缺结果/哈希错误、started损坏结果、旧/半初始化目录、链接路径、并发拒绝、源码与CLI执行中漂移，以及回调修改模型/预算/schedule不影响冻结执行。

硬退出夹具在result原子发布后以子进程退出码73终止，留下失活锁；恢复后验证已保存结果，只调用剩余一项，完整审计通过。原子JSON替换失败夹具确认旧记录原字节保持。测试产生的孤儿参与者目录在确认位于临时目录且具有自建前缀后清理。

所有驱动均为模拟或合成协议夹具，未启动真实参试CLI、模型服务或付费对照。原子替换不证明断电持久性，指纹不证明全部CLI依赖、闭包或远端模型别名一致，未知调用不声称恰好一次。本地DONE表示可提交版本，随后继续用户已授权的提交推送及该提交的远端CI核验，不能由本记录推断远端已通过。
