# 根目录别名修复交付

本轮修复Windows路径表示混用导致的PATH_ESCAPE误报。`beginWorkItem`、受控命令和guide入口使用真实root；快照保存同时接受原始root和规范root内的目录表示，继续经过原有越界/链接检查，不解析掉内部链接，也不要求输出目录预先存在。

最终产品快照：`079606ae4e661bb2c6c498befabfea088d5729707601b96bb2e8286dcf010b0d`。

Node.js 20.20.2、Windows 本地验证：

| 计划检查 | 结果 | 证据ID |
| --- | --- | --- |
| V1 根目录别名与写入边界 | 2/2，0跳过 | `f0955c0d-778c-4976-9436-5e2c9c730cf7` |
| V2 评测框架 | 15/15，0跳过 | `536eee5d-76c5-4baf-93d3-2a5c70ea7881` |
| V3 Runtime可靠性 | 12/12，0跳过 | `02043774-701d-4f9a-afd1-30ffb47baff6` |
| V4 Runtime其余20文件 | 128/128，0跳过 | `e9ec7997-ab83-4900-af49-490e0aad840b` |

Runtime合计142/142，清单核对覆盖全部22个测试文件，无重复或遗漏。V1在修复前两项均报PATH_ESCAPE，原始失败证据 `78f10d1c-f22d-41a3-8068-841a3a716c9f` 保留。独立上下文的[完整复核](review.md)通过，差异空白检查通过。

已读取远端Windows失败日志，确认之前评测15/15通过、Runtime70/140失败，路径调用栈与本地目录别名复现一致。此记录保存本地代码验收，远端完整工作流需在本次修复提交后核验；不得把本地结果替代远端结果。

没有修改既有权限规则、证据哈希和数据库格式，没有真实模型调用。此前的Git换行保护修复保留。临时验证目录清理曾被自动审批拒绝，因此保留现场，不通过其他删除方式绕过。
