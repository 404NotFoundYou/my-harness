# 为通用较弱模型提供确定性任务引导

增加独立只读 guide 模块，由已记录状态、计划依赖及现有门禁函数计算下一步；提供精简任务上下文、证据摘要与需要模型判断的字段。CLI 增加 guide --id [--task]，begin 结果提供入口提示。保留所有原命令和持久格式，不自动执行、不新增强制门禁。测试覆盖状态分流、证据归属、失败与恢复、权限判断和安装后 CLI。

数据库无影响依据：只读取既有工作项和证据，不新增持久化结构或数据库查询写入
